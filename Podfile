# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DetoxBear' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Purchases'
  pod 'AlertToast'
  pod 'SDWebImageSwiftUI'

  # Pods for DetoxBear

  target 'DetoxBearTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DetoxBearUITests' do
    # Pods for testing
  end

end
